# Web server for Crypto Impact

To run the web server in development mode, run `npm run dev` in the `server` directory.


To build server for production, run 
```powershell
npm run build
npm run start
```